> python manage.py makemigrations
> python manage.py migrate
> python manage.py createsuperuser

> ln -s /etc/nginx/sites-available/spiritumDuo.conf /etc/nginx/sites-enabled/

> python manage.py collectstatic

> /etc/init.d/nginx restart
> uwsgi --socket spiritumDuo.sock --module spiritumDuo.wsgi --chmod-socket=666
OR
> uwsgi --ini spiritumDuo_uwsgi.ini
> uwsgi --emperor --ini spiritumDuo_uwsgi.ini

    # python manage.py runserver 0.0.0.0:8080
    # uwsgi --http :8080 --wsgi-file test.py
    # uwsgi --http :8080 --module spiritumDuo.wsgi
    # uwsgi --emperor /etc/uwsgi-emperor/vassals --uid www-data --gid www-data

# Useful commands
'''
uwsgi --emperor /etc/uwsgi/vassals --uid www-data --gid www-data &
touch /etc/uwsgi/vassals/spiritumDuo_uwsgi.ini
python manage.py check --deploy

'''


Good videos
Full Django djProduction
- https://www.youtube.com/watch?v=ZpR1W-NWnp4

